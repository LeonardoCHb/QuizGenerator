import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  quiz: {
    width: "100%",
    height: "15rem",
    border: "1.5px",
    borderRadius: "1.5rem",
    alignItems: "center",
    display: "flex",
  },
}));
